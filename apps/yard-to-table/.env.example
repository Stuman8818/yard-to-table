# Optional Trellis origin override. Local development already defaults to http://localhost:3000.
# Use the deployed origin for production environments. No trailing slash.
NEXT_PUBLIC_TRELLIS_URL=https://trellis-software.vercel.app
